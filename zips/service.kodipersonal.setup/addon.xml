<?xml version="1.0" encoding="UTF-8"?>
<!-- Setup service. Applies the personal build defaults on first run and after each
     update, and declares the whole build stack as hard dependencies so Kodi installs
     the entire stack in one pass from the aggregated repository.aanotherworld when
     this addon is installed, and pulls any newly added ones when it updates. The
     floors are 0.0.1 (presence-only): we only need each addon present, not a specific
     version, and the skin and TMDb Helper enforce their own real version needs. Using
     0.0.1 instead of real floors is what avoids "dependency could not be satisfied"
     errors (script.module.jurialmunkey ships as 0.2.35, for example); these imports
     are NOT optional, because optional imports are never auto-installed by Kodi, which
     left Red Light and ResolveURL missing. Every id below resolves through the
     aggregated repo (jurialmunkey, The Red Repo, Gujal00 for ResolveURL, kodiyashimaru
     for the Magneto scraper) or Kodi's official repo (script.trakt, and the
     requests/pil sub-deps of Red Light and Magneto). The
     runtime checker (resources/lib/dependencies.py) re-enables anything disabled and
     notifies if a source was unreachable at install time. -->
<addon id="service.kodipersonal.setup"
       name="KODI Personal Setup"
       version="0.8.0"
       provider-name="AANOTHERWORLD">
  <requires>
    <import addon="xbmc.python" version="3.0.0" />
    <import addon="skin.arctic.fuse.3" version="0.0.1" />
    <import addon="plugin.video.themoviedb.helper" version="0.0.1" />
    <import addon="script.skinvariables" version="0.0.1" />
    <import addon="script.module.jurialmunkey" version="0.0.1" />
    <import addon="script.trakt" version="0.0.1" />
    <import addon="plugin.video.redlight" version="0.0.1" />
    <import addon="script.module.resolveurl" version="0.0.1" />
    <import addon="script.module.magneto" version="0.0.1" />
    <import addon="plugin.program.openwizard" version="0.0.1" />
  </requires>
  <extension point="xbmc.service" library="service.py" />
  <extension point="xbmc.addon.metadata">
    <summary lang="en_GB">Personal setup service for the lightweight build</summary>
    <description lang="en_GB">Applies the personal build defaults, TMDb Helper behavior and the source player slot, on first run and after each update so every device stays consistent. Copies the ThoughtStream colour set and background, applies the curated AF3 skin settings and view types, and deploys the home menu (Discover, For You, Netflix, Max, and a More Providers submenu) into Arctic Fuse 3 on every start so the look and menu self-heal after a skin update. Declares the build stack as dependencies so Kodi installs them automatically, and verifies them on startup, healing anything missing or disabled. Stores no secrets. Logs to its own profile folder.</description>
    <platform>all</platform>
    <license>MIT</license>
    <news>v0.8.0
- Fix Trakt not marking movies and episodes watched automatically. Three scrobblers were in play and none was writing: TMDb Helper's cannot see what the Red Light auto player is playing (that launch path never sets its playerstring), and Red Light deliberately steps aside when script.trakt is authorised with scrobbling on, so everything rode on script.trakt, which was not delivering. The setup service now turns script.trakt scrobbling off so Red Light is the single owner: it scrobbles with exact TMDb ids and writes watched history at 90 percent. TMDb Helper scrobbling is also off to prevent double scrobbles. Needs Trakt authorised inside Red Light with Trakt as its watched provider.
v0.7.9
- Devices now actually install repo updates. Kodi's addons.updatemode was set to notify-only with addon notifications switched off, so updates were found but never installed and never announced, which looks like the repo stopped publishing. The service now asserts automatic updates, and only writes when the value is wrong.
v0.7.8
- Add a Trakt auth health line to the log. TMDb Helper gates scrobbling on a runtime TraktIsAuth property that its service writes when the token refreshes; if the refresh fails, scrobbling stops silently while cached rows still look fine. The log now says whether the token is valid, expired or missing.
- Drop 4 settings keys that no longer exist in the current TMDb Helper schema (trakt_watchedinprogress, hide_unaired_movies, hide_unaired_episodes, pagemulti_library). They were inert; scrobbling settings are unchanged.
v0.7.7
- My Calendar now reliably leads with yesterday. The route slices the raw chronological response before it reverses, and the page size is capped at 160 items, so a 14 day window was being cut off mid-window and started around day 10. Window is now the past 7 days ending yesterday with the page size maxed, which fits with headroom.
v0.7.6
- OpenWizard now installs with the build, so every device has the same backup and export tool. The repository aggregates the OpenWizard repo (1.3.0) and the setup service requires plugin.program.openwizard. Use it to export device snapshots; do not restore a build with it, as that would overwrite what the repo and setup service maintain.
v0.7.5
- Magneto scrapers now initialise automatically on a new device. Magneto registers under Program add-ons and stayed uninitialised until opened by hand; the service now fires its default-providers action once per version and also enables Zilean. No manual scraper step on setup.
v0.7.4
- My Calendar now truly lists newest first with yesterday at the top: raised the page size (length=3, one 60-item page) because the calendar route paginates before reversing, which left the oldest week on page one. Removed sort params the route ignores.
- Continue Watching fixed for the current TMDb Helper: tmdb_type=both collapsed to movies only, so it is now two rows, Continue Watching (episodes) and Continue Movies, both Trakt playback progress newest paused first.
v0.7.3
- My Calendar now covers today-14 to today-1 (past two weeks ending yesterday). Corrected from startdate=-15 to startdate=-14, since Trakt returns days days from the start date so the old value ended two days ago.
v0.7.2
- Fix the For You Watchlist widget: it passed a literal tmdb_id of None, which made TMDb Helper query a bogus id and throw a pagination TypeError. Now uses the standard combined watchlist (info=trakt_watchlist, tmdb_type=both).
v0.7.1
- Add a scheduled texture-cache prune for data efficiency: removes textures unused for over 30 days, at most once every 7 days, via JSON-RPC so Kodi manages the locked database. Keeps the art cache from growing without bound.
- Log Kodi version and the texture database filename on each prune.
v0.7.0
- Add texture resolution caps (imageres 405, fanartres 720) to advancedsettings for lower memory use on low-RAM devices.
- One-time texture cache clear on update so the new caps take effect; runs once per version. First browse after is slower while art re-caches.</news>
  </extension>
</addon>
